import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nokta.tasarim',
  appName: 'Nokta Tasarım',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;